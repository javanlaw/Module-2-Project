name: elt_dbt_project
version: 1.0
config-version: 2

profile: elt_dbt_project

model-paths: ["models"]
target-path: "target"
clean-targets: ["target"]

models:
  elt_dbt_project:
    staging:
      materialized: view
    dimensions:
      materialized: table
    marts:
      materialized: table